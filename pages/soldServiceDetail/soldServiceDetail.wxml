<view class="top">
  <view class="order">
    <!-- <image class="status-icon" src="{{refund.statusIcon}}"></image> -->
    <view>{{refund.statusTxt}}</view>
    <view class="order-desc">
      <view class="order-desc-btm">
        <view>{{refund.statusDesc}}</view>
        <!-- <view class="pay" hidden="{{refund.status != '0'}}">需支付：￥{{refund.totalAmount}}</view>
        <view class="red" hidden="{{refund.status != '2'}}">{{refund.getOrderStatus == '0' ? '未接单': '已接单'}}</view> -->
      </view>
      <!-- <view hidden="{{refund.status != '2'}}">{{refund.failAuditReason}}</view> -->
    </view>
  </view>
</view>

<view class="middle">
  <view class="mid-cell" hidden="{{refund.status != '2'}}">
    <view class="label">
      拒绝原因:
    </view>
    <view>
     {{refund.failAuditReason}}
    </view>
  </view>
</view>
<view class="item">
  <!-- <view class="list" wx:for="{{refund.orderProductList}}" wx:for-item="child">
    <image src="{{child.productPicUrl}}" class="good"></image>
    <view class="left">
      <view class="name">
        {{child.productName}}
      </view>
      <view class="item-btm">
        <view>单价：￥{{child.price}}</view>
        <view class="good-num">数量：{{child.num}}{{child.standard}}</view>
      </view>
    </view>

  </view> -->
  <!-- <view class="total">
    <view class="mr-10">共{{refund.totalNum}}件</view>
    <view class="mr-10">实付</view>
    <view class="red">￥{{refund.totalAmount}}</view>
  </view> -->
</view>
<view class="cells">
  <view class="cell">
    <view class="label">
      订单号：
    </view>
    <view class="color-34">
      ￥{{refund.orderNo}}
    </view>
  </view>
  <view class="cell">
    <view class="label">
      订单金额：
    </view>
    <view class="color-34">
      ￥{{refund.refundAmount}}
    </view>
  </view>
  <view class="cell">
    <view>
      退单号：
    </view>
    <view>
      {{refund.refundNo}}
    </view>
  </view>
  <view class="cell">
    <view>退单金额：</view>
    <view>￥{{refund.refundAmount}}</view>
  </view>
  <view class="cell">
    <view>退单类型：</view>
    <view>{{refund.refundType}}</view>
  </view>
  <view class="cell">
    <view>申请时间：</view>
    <view>{{refund.createTime}}</view>
  </view>
  <view class="cell" >
    <view>售后原因</view>
    <view>{{refund.refundReason}}</view>
  </view>
  <view class="" >
    <view class="label">售后图片</view>
    <view>
      <image class="refund-pic" src="{{refund.refundPic}}"></image>
    </view>
  </view>
</view>
